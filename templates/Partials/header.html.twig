<!-- Start Navigation -->
<nav class="navbar navbar-default navbar-fixed white bootsnav bg-default">

	<div class="container">
		<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-menu">
			<i class="fa fa-bars"></i>
		</button>
		<!-- Start Header Navigation -->
		<div class="navbar-header">
			<a class="navbar-brand" href="{{ path('app_accueil') }}">
				<img src="{{ asset('assets/img/logo.png') }}" class="logo logo-scrolled" alt="" style="width:130px;">
			</a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="navbar-menu">
			{% if app.user %}
				<ul class="nav navbar-nav navbar-right" data-in="fadeInDown" data-out="fadeOutUp">
					<li>
						<a href="{{ path('app_logout') }}">
							<i class="ti-power-off" aria-hidden="true"></i>DECONNEXION</a>
					</li>

					{% if app.user.roles[1] == "ROLE_CANDIDATE" %}
						<li class="left-br">
							<a href="{{ path('Candidate_profile') }}" class="signin">Tableau de Bord</a>
						</li>
					{% elseif app.user.roles[1] == "ROLE_EMPLOYER" %}
						<li class="left-br">
							<a href="#" class="signin">Post un Employ</a>
						</li>
					{% endif %}
				</ul>
			{% else %}
				<ul class="nav navbar-nav navbar-right" data-in="fadeInDown" data-out="fadeOutUp">
					<li>
						<a href="{{ path('app_login') }}">
							<i class="fa fa-sign-in" aria-hidden="true"></i>Se Connectez</a>
					</li>
					<li class="left-br">
						<a href="{{ path('app_inscription') }}" class="signin">S'Inscrire</a>
					</li>
				</ul>
			{% endif %}
		</div>
		<!-- /.navbar-collapse -->
	</div>
</nav>
<div class="clearfix"></div>
